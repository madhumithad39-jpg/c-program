#include <stdio.h>
int main() {
    int a, n, sum = 0;
    scanf("%d", &a);
    temp = a;
    for (; n > 0; n /= 10) {
        int digit = n % 10;
        sum += digit * digit * digit;
    }
    if (sum == a) {
        printf("Yes");
    } else {
        printf("No");
    }
    return 0;
}
