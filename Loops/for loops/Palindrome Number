#include<stdio.h>
int main() {
    int n, reversed = 0, a;
    scanf("%d", &n);
    temp = n;
    for (; a> 0; a /= 10) {
        int digit = a% 10;
        reversed = reversed * 10 + digit;
    }
    if (reversed == n) {
        printf("Yes");
    } else {
        printf("No");
    }
}
