#include<stdio.h>
int main() {
    int n,m;
    int result=0;
    scanf("%d%d",&n,&m);
    for(int i=n; i<=m; i++) {
        if (i%n==0) {
            result+=i;
        }
    }
    printf("%d", result);
    return 0;
}
