#include <stdio.h>
int main() {
    int m, isPrime = 1;
    scanf("%d", &m);
    for (int i = 2; i <= m/ 2; i++) {
        if (m % i == 0) {
            isPrime = 0;
            break;
        }
    }
    if (isPrime) {
        printf("Prime");
    } else {
        printf("Not Prime");
    }
    return 0;
}
