#include<stdio.h>
#include<string.h>
int main() {
    char s[100];
    fgets(s, 100, stdin);
    int count=0;
    for (int i=0;s[i]!='\0';i++) {
        if (s[i]>='0' && s[i]<='9') {
            count++;
        }
    }
    printf("%d",count);
    return 0;
}
