#include <stdio.h>
#include <string.h>
int main() {
    char s[100];
    char manual[100];
    scanf("%s", s);
    int i;
    for(i = 0; s[i] != '\0'; i++) {
        manual[i] = s[i];
    }
    manual[i] = '\0';
    printf("%s", manual);
    return 0;
}
