#include <stdio.h>
#include <string.h>
int main() {
    char rev[100];
    int len=0;
    scanf("%s", &rev);
    for (int i=0;rev[i]!= '\0';i++) {
        len++;
    }
    for (int i=len-1;i>=0;i--) {
        printf("%c", rev[i]);
    }
    return 0;
}
