#include<stdio.h>
#include<string.h>
int main() {
    char s[100];
    fgets(s, sizeof(s), stdin);
    int count=0;
    for(int i = 0; s[i] != '\0'; i++)
        if(s[i] == ' ') {
            count++;
        }
    printf("%d", count);
    return 0;
}
