#include <stdio.h>
int power(int a, int b) {
    int sum=1;
    for (int i=1;i<=b;i++) {
        sum=sum*a;
    }
    return sum;
}
int main() {
    int a,b;
    scanf("%d %d",&a,&b);
    printf("%d",power(a,b));
    return 0;
}
