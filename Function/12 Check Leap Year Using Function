#include <stdio.h>
int leap(int year) {
        if (year%4==0 && year%400==0 || year%100!=0) {
            printf("Leap Year");
        }
        else {
            printf("No Leap Year");
        }
}
int main() {
    int year;
    scanf("%d",&year);
    leap(year);
    return 0;
}
