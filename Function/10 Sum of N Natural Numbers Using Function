#include<stdio.h>
int natural(int n) {
    int sum=0;
    for (int i=0;i<=n;i++) {
        sum=sum+i;
    }
    return sum;
}
int main() {
    int n;
    scanf("%d",&n);
    printf("%d",natural(n));
    return 0;
}
