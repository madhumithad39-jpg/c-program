#include<stdio.h>
int largest(int a,int b) {
    if(a>b) {
       return a;
    }
    else {
        return b;
    }
}
int main() {
    int a,b;
    int number;
    scanf("%d%d",&a,&b);
    number=largest(a,b);
    printf("%d",number);
    return 0;
}
