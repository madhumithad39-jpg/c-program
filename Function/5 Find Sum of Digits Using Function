#include<stdio.h>
int Digits(int n) {
    int sum=0;
    while(n!=0) {
        sum=sum+n%10;
        n=n/10;
    }
    return sum;
}
int main() {
    int n;
    int integer;
    scanf("%d",&n);
    integer=Digits(n);
    printf("%d",integer);
    return 0;
}
