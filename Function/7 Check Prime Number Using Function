#include<stdio.h>
int check(int n) {
    int check=0;
    for( int i=2;i<=n;i++) {
        if(n%i==0) {
            check=1;
            break;
        }
    }
    if(check==1) {
        printf("prime");
    }
    else {
        printf("not prime");
    }
}
int main() {
    int n;
    scanf("%d",&n);
    int number;
    check(n);
    return 0;
}
