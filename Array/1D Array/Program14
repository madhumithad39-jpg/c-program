#include <stdio.h>
int main() {
    int n;
    scanf("%d", &n);
    int arr[n];
    int min=0;
    for (int i = 0; i < n; i++) {
        scanf("%d", &arr[i]);
    }
    for (int i = 0; i < n; i++) {
        if (arr[i]>0) {
            if (min==0) {
                min=arr[i];
            }
            if (arr[i]<min) {
                min=arr[i];
            }
        }
    }
    if (min==0) {
        printf("No Positive");
    }
    else {
        printf("%d",min);
    }
    return 0;
}
