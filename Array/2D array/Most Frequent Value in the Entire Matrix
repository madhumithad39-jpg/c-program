#include <stdio.h>
int main() {
    int r, c;
    scanf("%d %d", &r, &c);
    int a[r][c];
    for (int i = 0; i < r; i++) {
        for (int j = 0; j < c; j++) {
            scanf("%d", &a[i][j]);
        }
    }
    int max = 0;
    int value = a[0][0];
    for (int i = 0; i < r; i++) {
        for (int j = 0; j < c; j++) {
            int count = 0;
            for ( int k = 0; k < r*c; k++) {
                int row = k / c;
                int col = k % c;
                if (a[i][j] == a[row][col]) {
                    count++;
                }
            }
            if (count > max) {
                max = count;
                value = a[i][j];
            }
        }
    }
    printf("%d", value);
    return 0;
}
