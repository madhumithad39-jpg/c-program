#include <stdio.h>

int main() {
    int account, years;

    scanf("%d", &account);
    scanf("%d", &years);

    switch (account) {
        case 1: 
            printf("Interest 4%%");
            break;

        case 2: 
            if (years <= 3)
                printf("Interest 5%%");
            else
                printf("Interest 7%%");
            break;
    }

    return 0;
}
