#include <stdio.h>
int  copyString(char *a, char *b) {
    while (*a != '\0') {
        *b = *a;
        a++;
        b++;
    }
    *b = '\0';
}
int main() {
    char a[100], b[100];
    fgets(a, sizeof(a), stdin);
    copyString(a, b);
    printf("%s", b);
    return 0;
}
